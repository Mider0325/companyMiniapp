<view class="container">
  <!-- 加载中提示 -->
  <view class="loading" wx:if="{{isLoading}}">
    <view class="loading-spinner"></view>
    <view class="loading-text">加载中...</view>
  </view>

  <!-- 轮播图 -->
  <swiper class="banner" indicator-dots="true" autoplay="true" interval="3000" duration="500" circular="true" wx:if="{{false}}">
    <block wx:for="{{bannerImages}}" wx:key="*this">
      <swiper-item>
        <my-image src="{{item}}" class="banner-image" mode="aspectFill" />
      </swiper-item>
    </block>
  </swiper>

  <!-- 公司简介 -->
  <view class="section company-intro" wx:if="{{!isLoading}}">
    <view class="section-title">公司介绍</view>
    <view class="company-detail">
      <view class="company-logo">
        <my-image src="/image/logo.jpg" mode="aspectFit" />
      </view>
      <view class="company-info">
        <view class="company-name">{{company.name}}</view>
        <view class="company-found">成立于 {{company.foundYear}} 年</view>
        <view class="company-desc">
          公司拥有国内外<text class="badge">国内工厂+越南工厂</text>多个生产基地，可供货芒果干{{company.capacity}}。<text class="badge">产品远销海内外</text>，<text class="badge">年销过亿</text>品牌实力源头工厂，品质放心。
        </view>
      </view>
    </view>
  </view>

  <!-- 源头工厂介绍 -->
  <view class="section origin-display" wx:if="{{!isLoading}}">
    <view class="section-title">源头工厂介绍</view>
    <view class="origin-list">
      <view class="origin-item">
        <view class="origin-title"> 国内生产基地 -- 广东·茂名 🇨🇳</view>
        <view class="origin-advantage">
          <text class="origin-icon">📍</text>
          国内工厂位于广东茂名，毗邻<text class="origin-key">菠萝的海·徐闻</text>和<text class="origin-key">中国荔乡 · 岭南-高州</text>。当地水果资源优质且丰富，原料储备充足。
        </view>
      </view>
      <view class="origin-item">
        <view class="origin-title">海外生产基地 -- 越南（新安 & 高岭）🇻🇳</view>
        <view class="origin-advantage">
          <text class="origin-icon">📍</text>
          工厂位于越南南部新安、高岭地区，毗邻柬埔寨，地处热带季风气候带，年均日照超<text class="origin-key">2500小时</text>，雨量充沛，是<text class="origin-key">全球顶尖芒果核心产区</text>。
        </view>
      </view>
    </view>
  </view>

  <!-- 产品展示 -->
  <view class="section product-display" wx:if="{{!isLoading && products.length > 0}}">
    <view class="section-title">产品展示</view>
    <view class="product-grid">
      <block wx:for="{{products}}" wx:key="id">
        <view class="product-item">
          <my-image src="{{item.image}}" class="product-image" mode="aspectFill" />
          <view class="product-info">
            <view class="product-name">{{item.name}}</view>
            <view class="product-desc">{{item.desc}}</view>
          </view>
        </view>
      </block>
    </view>
    <view class="more-btn" bindtap="navigateToProducts">查看全部产品</view>
  </view>

  <!-- 优势展示 -->
  <view class="section advantages" wx:if="{{!isLoading && advantages.length > 0}}">
    <view class="section-title">我们的优势</view>
    <view class="advantage-list">
      <block wx:for="{{advantages}}" wx:key="title">
        <view class="advantage-item">
          <view class="advantage-inner">
            <view class="advantage-icon">{{item.icon}}</view>
            <view class="advantage-title">{{item.title}}</view>
            <view class="advantage-desc">{{item.desc}}</view>
          </view>
        </view>
      </block>
    </view>
  </view>

  <!-- 联系我们 -->
  <view class="section contact" wx:if="{{!isLoading}}">
    <view class="section-title">联系我们</view>
    <view class="contact-info">
      <view class="contact-item">
        <text class="contact-label">董事长：温董</text>
        <text class="contact-icon" bindtap="callPhone" data-phone="18926730588">📞 拨打电话</text>
        <text class="contact-icon" bindtap="copyWechat" data-wechat="18926730588">💬 复制微信</text>
      </view>
      <view class="contact-item">
        <text class="contact-label">销售经理：温经理</text>
        <text class="contact-icon" bindtap="callPhone" data-phone="13035818146">📞 拨打电话</text>
        <text class="contact-icon" bindtap="copyWechat" data-wechat="13035818146">💬 复制微信</text>
      </view>
      <view class="contact-item">
        <text class="contact-label">电商总监：邓总</text>
        <text class="contact-icon" bindtap="callPhone" data-phone="18516774727">📞 拨打电话</text>
        <text class="contact-icon" bindtap="copyWechat" data-wechat="18516774727">💬 复制微信</text>
      </view>
    </view>
  </view>

  <!-- 页脚 -->
  <view class="footer" wx:if="{{!isLoading}}">
    <text>© 2025 广东茂名佰果园食品有限公司 版权所有</text>
  </view>
</view> 